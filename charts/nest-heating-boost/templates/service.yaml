apiVersion: v1
kind: Service
metadata:
  name: {{ include "nest-heating-boost.fullname" . }}
  labels:
    {{- include "nest-heating-boost.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nest-heating-boost.selectorLabels" . | nindent 4 }}
