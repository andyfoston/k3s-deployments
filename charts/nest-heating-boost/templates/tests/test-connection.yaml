apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nest-heating-boost.fullname" . }}-test-connection"
  labels:
    {{- include "nest-heating-boost.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nest-heating-boost.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
